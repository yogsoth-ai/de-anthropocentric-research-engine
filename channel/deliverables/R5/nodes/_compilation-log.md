# BASIS compilation log

## assess-sensitivity

Fan-in 7. Merged weight, ranking, method, conclusion, fragility, and meta-analysis sensitivity transforms. A=0, B=5, C=0; unresolved provenance=3 (`Pass3/*`). Fixed source criteria preserved: ±20% perturbation, τ bands, ≥2 rank-difference positions, and 0.0–1.0 fragility index.

## evaluate-compatibility

Fan-in 6. Merged logical, empirical, normative, CCA reduction, and combination-feasibility checks. A=0, B=2, C=0; unresolved provenance=1 (`creative-ideation/consistency-pair-evaluation`).

## score-object

Fan-in 6. Merged 15 scoring sources into one parameterized scoring SOP. A=0, B=7, C=0; unresolved provenance=7 (four `Pass4/*` entries and three unresolved method/strategy entries). Final state: all rubrics remain caller-owned in `Parameterization`; no shared rubric directory.

## surface-assumptions

Fan-in 6. Merged explicit/implicit assumption enumeration and criticality assessment. A=0, B=1, C=0; unresolved provenance=3 (concept entries).

## detect-coverage-gap

Fan-in 5. Merged matrix, graph, taxonomy, IP, orphan, and weak-link gap detection. A=0, B=3, C=0; unresolved provenance=3 (`Pass3/*`). Relative coverage uses the six audit fields plus direction and threshold rationale.

## analyze-temporal-trajectory

Fan-in 4. Merged filing trends, progress curves, timeline projection, persistence windows, and optional forecasts. A=0, B=2, C=0; unresolved provenance=3 (`Pass3/*`). Progress-curve minimum yield remains 2 curves, 3 years, 1 inflection, 2 headroom estimates.

## enumerate-dimension-values

Fan-in 4. Merged representative, experimental, boundary, pathological, and adversarial value generation. A=0, B=2, C=0; unresolved provenance=3 (`Pass3/*`). Preserved 3–5 representative values and 2–5 experimental levels.

## identify-variables

Fan-in 4. Merged variable, factor, parameter, condition, and assumption enumeration with role annotations. A=0, B=1, C=0; unresolved provenance=1 (`hypothesis-formation/variable-identification`).

## apply-perturbation

Fan-in 3. Merged controlled variation, assumption perturbation, ablation, and single-factor removal. A=0, B=1, C=0; unresolved provenance=1 (concept entry).

## canonicalize-entity

Fan-in 3. Merged concept aliasing, near-duplicate merging, and assignee normalization. A=0, B=3, C=0; unresolved provenance=3 (`Pass3/*` and `Pass4/*`). Preserved scan-at-least-10 and score >7.0 candidate flag.
