# Ladder-Foundry — Implementation Blueprint Phase

> Created: 2026-06-30 12:55
> Topic: Labeled research-quality ladder generator (was "triple-cc")
> Phase: Blueprint + component inventory + build-order design

## Plan Context

This project (`ladder-foundry`) builds the system formerly called "triple-cc". Renamed by purpose: its whole job is to FORGE a labeled, monotone good→bad LADDER of research samples whose label = the known generating condition (the PolicyCard that produced it), sidestepping the no-ground-truth problem that GitHub Discussion #22 raised. The mechanism (three nested CCs as a pseudo-NN training loop) is an implementation detail, not the name.

The user is resuming this work after the disc#22 brainstorm. Plan: implement it ONE COMPONENT AT A TIME, run each in isolation to confirm it satisfies, then assemble and run the whole. This Phase produces the BLUEPRINT: a complete component inventory + a mermaid dependency/design-order/build-order graph, so the piece-by-piece build has a map.

### Source design (3 docs, now under `D:\YOGSOTH-AI\context\self-iteration\2026-06-07-triple-cc-system\`)

- `2026-06-06-triple-cc-architecture-design.md` — system-design authority (1448 lines): the ASCII pipeline spine + 11-event trace + 3 trainable weights (① axis_prose / ② interp_params / ③ assembler_params) + frozen_label (locked rank+coord) + loss-1 injection-fidelity (per-run) / loss-2 ladder-quality-order (per-topic) + backprop "attribute-first" (only intelligent point) + W5 check-blind + privacy red line.
- Plan A `remote-env-from-scratch` (10 Task), Plan B `system-build` (B1–B7, TDD), Plan C `assemble-run-supervise` (C0–C4). Strict A→B→C order.

### disc#22 design kernel (recalled from memory, load-bearing)

- The persona-LADDER's label = generating condition (cause, NOT a judgement of the effect). This is the root that escapes no-ground-truth.
- 5 LABEL axes {A1 substance-demand, A2 legitimacy(secondary), A3 operationalization-insistence, A4 premise-corrigibility=PG engine, A5 generativity=NG engine} + 1 CONFOUND {B1 framing}.
- Three verdict types (formal / world / taste); taste is ORTHOGONAL to mimetic status (regress crowd-position out, neither chase nor flee); the 32 checks are all shallow text-structure FORMAL verdicts. The loss skills here are check-blind: they judge "was the generating condition faithfully executed / is the ladder monotone", NOT research quality.

### Verified facts this session (correct the plans where they conflict)

1. **`formated-specs` / `formated-results` / `injection-fidelity` / `ladder-quality-order` do NOT exist in the main repo** (`skills/` has `writing-specs` + 920 skills, none `formated-*`). They exist only as concept versions in the old `2026-06-06-probe-pretrain/skills/` (which we do NOT inherit code from). So Plan B's claim "formated-* already exist, B only interfaces the contract" is WRONG; design-doc L891 is right: **they are to-be-built components**. This is a real gap the blueprint must surface.
2. trace schema source = `D:\YOGSOTH-AI\presentations\probe-pretrain-trace\index.html` (11 events).
3. `context-init` / `context-checkpoint` / `context-review` skills exist in repo (convention reusable).
4. Repo is on branch `main`; ladder-foundry created fresh (gitignore: runs/, weights/*.json, env.sh, keys).

### HARD CONSTRAINTS carried from CLAUDE.md (do NOT violate)

- NO `-p`/`--resume`/`--session-id`/`--allowedTools` anywhere. All CCs are normal interactive REPLs.
- sim/exec freshly spawned EACH run; only optimizer runs continuously (tmux). Parent CC starts child by running `claude` directly in its own Bash tool (IS_SANDBOX=1 + child CONFIG_DIR + cd). NO driver script / NO PTY / NO tmux for children.
- 3 trainable weights are JSON data segments; `.py` are pure read-only functions. frozen_label locked. Collapse-perturbation uses ONLY B1/expression, NEVER A1–A5.
- W5 leak boundary: generation + loss skills are check-blind. Privacy red line: CC log paths + key values NEVER in committed artifacts. Write/Edit <13000 chars.
- 4-layer DARE architecture invariant; D1–D5-only evaluation; build from scratch (do not inherit old probe-pretrain code).
