---
name: constructive-rebellion
description: Build constructive alternatives from destructive negation. Transform
  violated assumptions into viable innovation directions.
execution: subagent
prompt: ./prompt.md
input: negated_assumptions (array), original_context (string)
dependencies:
  sops:
  - spawn-agent
---

# Constructive Rebellion

Build constructive alternatives from destructive negation.

## Execution

Subagent — spawned via subagent-spawning/spawn-agent skill.

## Why Subagent

Constructive rebellion is the critical bridge from destruction to creation — it requires both creative generativity and disciplined feasibility thinking. Benefits from dedicated focus to avoid either premature dismissal or unconstrained fantasy.

<!-- BEGIN available-tables (generated) -->

## Available SOPs

可选,无固定顺序;最终叶子终为 sop。

| SOP | 何时用 |
| --- | --- |
| spawn-agent | Spawn a customized CC subagent with full MCP tool access. Used by SOPs that declare execution: subagent. |

<!-- END available-tables (generated) -->
