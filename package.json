{
  "name": "dare",
  "version": "2.0.0",
  "private": true,
  "description": "DARE — De-Anthropocentric Research Engine",
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "npm test -w packages/scholar -w packages/web -w packages/agents -w packages/ss",
    "build": "npm run build --workspaces --if-present"
  },
  "license": "Apache-2.0"
}
